#include <iostream>
using namespace std;
int main()
{
    double a,b,product;
    cout << "enter two numbers:";
    cin >> a >> b;
    product = a * b;
    cout << "product is:" << product;
    
    return 0;
}
